<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SuitmediaProject" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/gblue</item>
    </style>

    <style name="Theme.SuitmediaProject" parent="Base.Theme.SuitmediaProject" />
</resources>